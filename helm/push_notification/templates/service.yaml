apiVersion: v1
kind: Service
metadata:
  name: {{ include "push_notification.fullname" . }}
  labels:
    {{- include "push_notification.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "push_notification.selectorLabels" . | nindent 4 }}
