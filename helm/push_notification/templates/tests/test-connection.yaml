apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "push_notification.fullname" . }}-test-connection"
  labels:
    {{- include "push_notification.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "push_notification.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
